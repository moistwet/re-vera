"""Backend test suite.

A package rather than a loose directory so test modules can import the shared
constants in :mod:`tests.conftest` (``from .conftest import TEST_DAILY_CAP``)
alongside using its fixtures.
"""
